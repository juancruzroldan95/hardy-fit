---
import { Image } from "astro:assets";
import heroImage from "../assets/images/todo-es-energia-contenida.webp";

export interface Props {
  titleColor?: string;
  titleSize?: string;
  descriptionColor?: string;
}

const {
  titleColor = "text-black",
  titleSize = "text-5xl md:text-6xl",
  descriptionColor = "text-black",
} = Astro.props;
---

<div class="relative py-24 overflow-hidden scrolling-section opacity-0">
  <div class="absolute inset-0 bg-cream"></div>
  <div
    class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(195,33,54,0.08),transparent_55%)]"
  >
  </div>
  <div class="relative z-10 max-w-7xl mx-auto px-6 lg:px-8">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
      <div>
        <h1
          class={`text-center lg:text-left ${titleSize} font-header font-extrabold uppercase ${titleColor} mb-6 leading-tight`}
        >
          Alimentá tu instinto
        </h1>
        <p class={`text-xl ${descriptionColor} leading-relaxed mb-8`}>
          Hardy es impulso, empuje, movimiento hacia la conquista de nosotros
          mismos. Es ir siempre hacia adelante, reconociendo nuestros miedos y
          limitaciones para conquistarlos a ellos también.
        </p>
        <p class={`text-xl ${descriptionColor} leading-relaxed mb-8`}>
          El impulso nos permite estar en armonía, manteniéndonos positivos y
          siempre activos. Es una competencia continua con nosotros mismos; la
          meta es hacer lo que nos apasiona con coraje, elevando nuestro
          carácter, teniendo el control sobre cuerpo y mente.
        </p>
      </div>
      <div class="hidden lg:block relative mt-10 lg:mt-0">
        <div
          class="aspect-square w-full max-w-xl mx-auto rounded-2xl shadow-xl overflow-hidden"
        >
          <Image
            src={heroImage}
            alt="Hardy athlete training with intensity"
            class="w-full h-full object-cover"
            loading="eager"
            quality={90}
          />
        </div>
      </div>
    </div>
  </div>
</div>
